<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <!-- Fonts bundled with the Lambda package live here. -->
  <dir>/var/task/functions/social/fonts</dir>
  <cachedir>/tmp/fonts-cache</cachedir>
  <!-- Map the SVG's "Gooners" family onto whatever bold sans we bundle. -->
  <match target="pattern">
    <test name="family"><string>Gooners</string></test>
    <edit name="family" mode="assign" binding="strong"><string>DejaVu Sans</string></edit>
  </match>
</fontconfig>
